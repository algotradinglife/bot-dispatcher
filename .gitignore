# Runtime configuration and state are local. Only the sanitized example is tracked.
dispatcher.yaml
*.state.json
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
# 测试已纳入版本库（2026-08-11 用户拍板：PI 返工要求绿 CI，测试入库）
